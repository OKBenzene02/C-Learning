/*  =======MATRIX======
MATRIX ADDITON
Date: 29-12-2020
time: 17:00
*/
/*
#include<stdio.h>
#define p 3
#define q 3
#define r 3
#define s 3
int main()
{
    int a[p][q], b[r][s], c[p][s];

    if ((p == r) && (q == s))
    {
        for (int i = 0; i < p; i++)
        {
            for (int j = 0; j < q; j++)
            {
                printf("please enter the values of matrix A[%d][%d]: \n", i, j);
                scanf("%d", &a[i][j]);
            }
        }
         for (int i = 0; i < r; i++)
        {
            for (int j = 0; j < s; j++)
            {
                printf("please enter the values of matrix B[%d][%d]: \n", i, j);
                scanf("%d", &b[i][j]);
            }
        }
         for (int i = 0; i < p; i++)
        {
            for (int j = 0; j < s; j++)
            {
                c[i][j] = a[i][j] + b[i][j];
            }
        }
        printf("the addition of matrix A and B: \n");
         for (int i = 0; i < p; i++)
        {
            for (int j = 0; j < s; j++)
            {
                printf("\t %d", c[i][j]);
            }
            printf("\n");
        }

    }
    else
    {
        printf("the matrix addition is not possible.\n");
    }

    return 0;
}
*/
